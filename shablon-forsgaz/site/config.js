/* =====================================================================
   КОНФИГ САЙТА — всё, что меняется под конкретный сервис
   без правки кода. Здесь же числа для калькулятора.
   Данные организации взяты из карточки на Яндекс.Картах
   (Force-Gaz, установка ГБО, пос. Образцово, Садовая ул., 46Л).
   ===================================================================== */
window.CFG = {

  /* --- Контакты. ОДИН номер на все мессенджеры. --- */
  contacts: {
    phone:        '79699735873',
    phoneDisplay: '+7 (969) 973-58-73',
    telegram:     '+79699735873',     // Telegram открывается по номеру
    max:          '',
    waText:       'Здравствуйте! Пишу с сайта Force-Gaz. Хочу записаться.',
  },

  /* --- Карточка организации на Яндекс.Картах --- */
  yandex: {
    org:     'https://yandex.ru/maps/org/force_gaz/68565588783/',
    reviews: 'https://yandex.ru/maps/org/force_gaz/68565588783/reviews/',
    route:   'https://yandex.ru/maps/?mode=routes&rtext=~55.943800%2C37.925533&rtt=auto',
  },

  /* --- Прочие ссылки для подвала. Соцсетей у сервиса нет:
         всё общение идёт через телефон, WhatsApp и Telegram. --- */
  links: {
    avito:        '',
    avitoReviews: '',
  },

  /* --- Реквизиты организации (для подвала).
         Пока не заполнены — в подвале строка с ними не выводится. --- */
  legal: {
    orgName: 'Force-Gaz',
    ogrnip: '',
    inn:    '',
    okpo:   '',
  },

  /* --- График работы. По нему в шапке горит зелёный/красный индикатор.
         Пн–Сб 09:00–18:00, воскресенье — выходной.
         Дни недели: 0=вс, 1=пн, 2=вт … 6=сб. Время — в минутах. --- */
  schedule: {
    tz:    'Europe/Moscow',
    days:  [1, 2, 3, 4, 5, 6],   // понедельник–суббота
    from:  9 * 60,               // 09:00
    to:    18 * 60,              // 18:00
  },

  /* --- Блок 1: сколько держится каждое состояние, мс --- */
  hero: {
    durations: { simple: 4000, china: 8000, commercial: 8000 },
  },

  /* --- Блок 1: счётчик обслуженных машин.
         Пока в text стоит готовая строка — она и выводится, как есть.
         Уберите text (или сделайте пустым), и снова заработает растущий
         счётчик: base + прирост perDay за каждые сутки с даты since. --- */
  counter: {
    text:      '5000+',
    base:      5000,
    perDay:    1.5,
    since:     '2026-09-01',
  },

  /* --- Блок 3: автопереключение подблоков, мс --- */
  tabs: { autoSwitchMs: 15000 },
};
